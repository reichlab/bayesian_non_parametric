var x[t_max], y[t_max], alpha
model
{
  x[1] ~ dnorm(mean_x_init,1)
  y[1] ~ dnorm(x[1],1)
  alpha ~ dexp(1.5 )
  
  for (t in 2:t_max)
  {
    x[t] ~ dnorm(x[t-1],1)
    y[t] ~ dnorm(alpha*x[t],1)
  }
}
